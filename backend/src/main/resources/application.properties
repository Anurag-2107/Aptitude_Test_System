# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/aptitude_test_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Nishant@09

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL Scripts Configuration
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Server Port
server.port=8080

# Logging
logging.level.com.aptitudetest=DEBUG

# Disable H2 console (since we're using MySQL)

# TEMPORARY: Disable Spring Security completely
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Keep your existing database config
spring.datasource.url=jdbc:mysql://localhost:3306/aptitude_test_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Nishant@09
spring.jpa.hibernate.ddl-auto=update